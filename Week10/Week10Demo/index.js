// index.js — entry point (for React Native CLI). For Expo you can ignore this file.

import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('main', () => App);
